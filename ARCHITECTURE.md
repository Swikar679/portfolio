# Architecture Overview

## 🏗️ Application Architecture

### Before (Traditional Express Server)
```
┌─────────────────────────────────────────┐
│         Single Server Instance          │
│  ┌────────────────────────────────────┐ │
│  │         Express Server             │ │
│  │  ┌──────────┐      ┌────────────┐ │ │
│  │  │  Routes  │──────│  Database  │ │ │
│  │  └──────────┘      └────────────┘ │ │
│  │       │                            │ │
│  │  ┌────▼─────┐                     │ │
│  │  │  Static  │                     │ │
│  │  │  Files   │                     │ │
│  │  └──────────┘                     │ │
│  └────────────────────────────────────┘ │
└─────────────────────────────────────────┘
         Always running on one server
```

### After (Vercel Serverless)
```
┌──────────────────────────────────────────────────────────┐
│                    Vercel Platform                       │
│                                                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │           Static Frontend (CDN)                 │   │
│  │  ┌──────────────────────────────────────────┐   │   │
│  │  │  React App (dist/public/)                │   │   │
│  │  │  - HTML, CSS, JS                         │   │   │
│  │  │  - Images, Videos                        │   │   │
│  │  │  - Served from global CDN                │   │   │
│  │  └──────────────────────────────────────────┘   │   │
│  └─────────────────────────────────────────────────┘   │
│                                                          │
│  ┌─────────────────────────────────────────────────┐   │
│  │        Serverless API Functions                 │   │
│  │                                                  │   │
│  │  ┌────────────────────────────────────────┐    │   │
│  │  │  /api/reels/index.ts                   │    │   │
│  │  │  - GET /api/reels                      │    │   │
│  │  │  - POST /api/reels                     │    │   │
│  │  └────────────────────────────────────────┘    │   │
│  │                                                  │   │
│  │  ┌────────────────────────────────────────┐    │   │
│  │  │  /api/reels/[id].ts                    │    │   │
│  │  │  - GET /api/reels/:id                  │    │   │
│  │  └────────────────────────────────────────┘    │   │
│  │                                                  │   │
│  │  ┌────────────────────────────────────────┐    │   │
│  │  │  /api/admin/seed.ts                    │    │   │
│  │  │  - POST /api/admin/seed                │    │   │
│  │  └────────────────────────────────────────┘    │   │
│  │                                                  │   │
│  │  Each function runs independently              │   │
│  │  Scales automatically based on traffic         │   │
│  └─────────────────────────────────────────────────┘   │
│                         │                                │
│                         ▼                                │
│  ┌─────────────────────────────────────────────────┐   │
│  │         Database Connection Pool                │   │
│  │  - Optimized for serverless                     │   │
│  │  - Max 1 connection per function                │   │
│  │  - Reuses connections when possible             │   │
│  └─────────────────────────────────────────────────┘   │
└──────────────────────────────────────────────────────────┘
                         │
                         ▼
        ┌────────────────────────────────┐
        │   PostgreSQL Database          │
        │   (Vercel Postgres/Neon/etc)   │
        │                                │
        │   ┌──────────────────────┐    │
        │   │   reels table        │    │
        │   │   - id               │    │
        │   │   - title            │    │
        │   │   - description      │    │
        │   │   - videoUrl         │    │
        │   │   - posterUrl        │    │
        │   │   - isFeatured       │    │
        │   └──────────────────────┘    │
        └────────────────────────────────┘
```

## 🔄 Request Flow

### Frontend Request (e.g., GET /)
```
User Browser
    │
    ▼
Vercel CDN (Global Edge Network)
    │
    ▼
Static Files (dist/public/index.html)
    │
    ▼
React App Loads
    │
    ▼
Client-side Routing (wouter)
```

### API Request (e.g., GET /api/reels)
```
User Browser
    │
    ▼
Vercel Edge Network
    │
    ▼
Serverless Function (/api/reels/index.ts)
    │
    ├─ Cold Start? (if function not warm)
    │  └─ Initialize runtime (~100-500ms)
    │
    ▼
Database Connection Pool
    │
    ▼
PostgreSQL Database
    │
    ▼
Query Results
    │
    ▼
JSON Response to Browser
```

## 📊 Scaling Behavior

### Traditional Server
```
Traffic:  ▁▁▁▁▃▃▃▅▅▅▇▇▇█████▇▇▇▅▅▅▃▃▃▁▁▁▁
Server:   ████████████████████████████████
Cost:     ████████████████████████████████
          Always running, fixed cost
```

### Serverless
```
Traffic:  ▁▁▁▁▃▃▃▅▅▅▇▇▇█████▇▇▇▅▅▅▃▃▃▁▁▁▁
Functions:▁▁▁▁▃▃▃▅▅▅▇▇▇█████▇▇▇▅▅▅▃▃▃▁▁▁▁
Cost:     ▁▁▁▁▃▃▃▅▅▅▇▇▇█████▇▇▇▅▅▅▃▃▃▁▁▁▁
          Scales with traffic, pay per use
```

## 🌍 Global Distribution

```
        ┌─────────────────────────────────┐
        │      Vercel Global Network      │
        └─────────────────────────────────┘
                      │
        ┌─────────────┼─────────────┐
        │             │             │
        ▼             ▼             ▼
   ┌────────┐   ┌────────┐   ┌────────┐
   │  USA   │   │ Europe │   │  Asia  │
   │  Edge  │   │  Edge  │   │  Edge  │
   └────────┘   └────────┘   └────────┘
        │             │             │
        └─────────────┼─────────────┘
                      │
                      ▼
            Static files served from
            nearest edge location
                      │
                      ▼
            API functions run in
            optimal region
```

## 💾 Data Flow

### Creating a Reel
```
1. User fills form in React app
        │
        ▼
2. POST /api/reels with JSON data
        │
        ▼
3. Serverless function validates data
        │
        ▼
4. Insert into PostgreSQL database
        │
        ▼
5. Return new reel with ID
        │
        ▼
6. React app updates UI
```

### Loading Reels
```
1. React component mounts
        │
        ▼
2. useQuery fetches GET /api/reels
        │
        ▼
3. Serverless function queries database
        │
        ▼
4. Returns array of reels
        │
        ▼
5. React renders reel cards
```

## 🔐 Security Layers

```
┌─────────────────────────────────────┐
│         Vercel Platform             │
│  ┌───────────────────────────────┐  │
│  │  DDoS Protection              │  │
│  └───────────────────────────────┘  │
│  ┌───────────────────────────────┐  │
│  │  SSL/TLS Encryption           │  │
│  └───────────────────────────────┘  │
│  ┌───────────────────────────────┐  │
│  │  CORS Headers                 │  │
│  └───────────────────────────────┘  │
│  ┌───────────────────────────────┐  │
│  │  Environment Variables        │  │
│  │  (DATABASE_URL hidden)        │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

## 🎯 Key Benefits

| Aspect | Benefit |
|--------|---------|
| **Performance** | Global CDN, edge caching |
| **Scalability** | Auto-scales to millions of requests |
| **Cost** | Pay only for what you use |
| **Deployment** | Git push = automatic deploy |
| **Maintenance** | No server management needed |
| **Reliability** | Built-in redundancy and failover |

## ⚡ Performance Characteristics

### Cold Start
- First request after idle: ~100-500ms
- Subsequent requests: ~10-50ms
- Functions stay warm for ~5-15 minutes

### Database Connections
- Connection pooling enabled
- Max 1 connection per function instance
- Connections reused across invocations

### Static Assets
- Served from global CDN
- Cached at edge locations
- ~20-50ms response time globally
